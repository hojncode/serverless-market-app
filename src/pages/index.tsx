import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div className="bg-red-500 flex">
      <h1 className="text-black">it works</h1>
    </div>
  );
};

export default Home;
